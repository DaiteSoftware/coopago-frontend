import React from "react";

export const FormRegisterWrapper = ({ children }) => {
  return (
  <div className="flex container items-center justify-center h-full w-full">
    {children}
  </div>);
};
